[pytest]
# Two suites, deliberately separate roots:
#   backend/tests   the Phase 0 engine, which must never touch AWS
#   workload/tests  the Phase 1 experiment driver, which may, but only when asked
testpaths = backend/tests workload/tests
